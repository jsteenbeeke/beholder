function renderToken(canvasId, token) {
	var src = token.src; // string
	var borderType = token.border_type; // string (enum)
	var borderIntensity = token.border_intensity; // string (enum)
	var label = token.label; // string
	var x = token.x; // int
	var y = token.y; // int
	var width = token.width; // int
	var height = token.height; // int
	
	// TODO actually render token
}